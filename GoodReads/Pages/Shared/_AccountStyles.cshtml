@page
@model GoodReads.Shared._AccountStylesModel
@{
}

<style>
    .center-screen {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh; /* Full viewport height */
    }

    .login-container {
        min-width: 30vw;
        max-width: 60vw; /* Adjust as needed */
        padding: 20px;
        background-color: #ffffff; /* Optional: white background */
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Optional: subtle shadow */
        border-radius: 8px; /* Optional: rounded corners */
    }

    .form-floating input {
        width: 100%; /* Ensure inputs take the full width of their container */
    }
</style>